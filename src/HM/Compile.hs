
module Compile where

import HM.Language

-- Untyped Lambda Calculus will be our target language
-- data Core = Var String
--           | Const Lit
--           | Lam String (Core)
--           | App (Core) (Core)

-- compile :: ExpTs -> Core
-- compile (EVar ied) = Var 
